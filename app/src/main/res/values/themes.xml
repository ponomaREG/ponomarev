<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Ponomarev" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primaryVariant</item>
        <item name="colorOnPrimary">@color/primaryOn</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondaryVariant</item>
        <item name="colorOnSecondary">@color/secondaryOn</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- PopupMenu Theme -->
    <style name="Theme.Ponomarev.PopupMenu" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="popupMenuBackground">@color/primary</item>
    </style>
    <!-- FloatingButton Theme -->
    <style name="Theme.Ponomarev.FloatingButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="tint">@color/floatingButtonIconTint</item>
        <item name="fabCustomSize">48dp</item>
        <item name="maxImageSize">36dp</item>
        <item name="elevation">5dp</item>
        <item name="backgroundTint">@color/floatingButtonBackgroundTint</item>
    </style>
    <!-- TabLayout Theme -->
    <style name="Theme.Ponomarev.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/secondary</item>
        <item name="tabTextAppearance">@style/Theme.Ponomarev.TabLayout.TextAppearance</item>
        <item name="tabSelectedTextColor">@color/secondary</item>
        <item name="tabTextColor">@color/primaryTextColor</item>
    </style>
    <!-- TabLayout TextAppearance -->
    <style name="Theme.Ponomarev.TabLayout.TextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textSize">13sp</item>
    </style>
</resources>
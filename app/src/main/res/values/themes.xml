<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Ponomarev" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@color/primaryLightColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/secondaryLightColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- PopupMenu Theme -->
    <style name="Theme.Ponomarev.PopupMenu" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="popupMenuBackground">@color/primaryColor</item>
    </style>
    <!-- FloatingButton Theme -->
    <style name="Theme.Ponomarev.FloatingButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="tint">@color/iconColor</item>
        <item name="fabCustomSize">48dp</item>
        <item name="maxImageSize">36dp</item>
    </style>
    <!-- TabLayout Theme -->
    <style name="Theme.Ponomarev.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/secondaryColor</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabTextAppearance">@style/Theme.Ponomarev.TabLayout.TextAppearance</item>
        <item name="tabSelectedTextColor">@color/secondaryColor</item>
    </style>
    <!-- TabLayout TextAppearance -->
    <style name="Theme.Ponomarev.TabLayout.TextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textSize">12sp</item>
    </style>
</resources>